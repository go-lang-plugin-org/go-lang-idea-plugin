<b>func Println(a ...interface{}) (n int, err error)</b></b><br/>Println formats using the default formats for its operands and writes to standard output.<br/>Spaces are always added between operands and a newline is appended.<br/>It returns the number of bytes written and any write error encountered.