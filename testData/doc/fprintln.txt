<b>func Fprintln(w io.Writer, a ...interface{}) (n int, err error)</b></b><br/>Fprintln formats using the default formats for its operands and writes to w.<br/>Spaces are always added between operands and a newline is appended.<br/>It returns the number of bytes written and any write error encountered.