package main

func Ok1(a int, b ...int) int {
    return a + len(b)
}

func NotLast(/*begin*/a ...int/*end.Can only use ... as final argument in list*/, b int) int {
    return len(a) + b
}

func Result(a int) (/*begin*/n ...int/*end.Cannot use ... in output argument list*/) {
    return
}
